import itertools
def generate():
    for i in itertools.count(1):
        yield from [str(i)]
n = int(input('число: '))
print(*itertools.islice(generate(), n))
#через оператор for i in решение выдавало ошибку, поэтому добавила к заданию итератор
